<%= form_for(intensity) do |f| %>
  <% if intensity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(intensity.errors.count, "error") %> prohibited this intensity from being saved:</h2>

      <ul>
      <% intensity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :level %>
    <%= f.number_field :level %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
